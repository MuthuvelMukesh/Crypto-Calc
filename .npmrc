# NPM Configuration for Crypto-Calc

# Use package-lock.json (recommended for projects)
package-lock=true

# Ensure clean installs
save=true
save-exact=false

# Legacy peer dependency handling
legacy-peer-deps=true

# Registry
registry=https://registry.npmjs.org/
